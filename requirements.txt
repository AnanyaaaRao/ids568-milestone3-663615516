# ============================================================
# requirements.txt - Pinned dependencies for reproducibility
# Generated for Python 3.11
# ============================================================

# Core ML
scikit-learn==1.4.2
numpy==1.26.4
pandas==2.2.2
pyarrow==16.0.0       # parquet support for pandas

# MLflow
mlflow==2.13.0
alembic==1.13.1        # MLflow dependency

# Airflow (install separately - see README for instructions)
# apache-airflow==2.9.1
# Note: Airflow has complex dependency requirements.
# Install via: pip install "apache-airflow==2.9.1" --constraint
#   https://raw.githubusercontent.com/apache/airflow/constraints-2.9.1/constraints-3.11.txt

# Utilities
requests==2.32.2
python-dotenv==1.0.1

# Testing / CI
pytest==8.2.0
pytest-cov==5.0.0